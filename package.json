{
  "name": "@mapapa/console-tests",
  "version": "1.0.0",
  "description": "Integration Tests, Canary Tests, and Load Tests logic for Mapapa Console",
  "scripts": {
    "test": "echo 'running tests'",
    "build": "echo 'building the packages'",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run"
  },
  "dependencies": {
    "cypress": "^12.0.2"
  },
  "devDependencies": {
    "@types/node": "^18.11.13",
    "typescript": "^4.9.4"
  }
}